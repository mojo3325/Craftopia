# Copy to `.env` for local use (never commit `.env`).
# This repo supports configuring keys via `Config.xcconfig` (ignored) or via the app Settings (Keychain).

# Cerebras
# CEREBRAS_API_KEY=csk-your-key-here
